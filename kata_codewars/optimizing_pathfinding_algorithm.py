def get_number_of_reachable_fields(grid, rows, columns, start_row, start_column):
    # Your magic here...
    print(grid[start_row][start_column])
    if grid[start_row][start_column] == 0:
        return 0
    #create grid
    gameboard = [rows][columns]
    # Draw board - Blocked
    for i in range(0,rows)


    #try to move left or right

